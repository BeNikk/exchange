export const PRICES: Record<string, { price: number; decimal: number }> = {};
export const BALANCES: Record<string, { usd: number; locked: number}> = {}; // userId -> {usd, locked};
export const openOrders: any = [];
